query GetLoan($loanId: ID!) {
  getLoan(loanId: $loanId) {
    id
    name
    startAt
    paymentsPerYear
    paymentAmount
    principal
    completedPayments {
      principalPayment
      interestPayment
      dateTime
    }
  }
}
